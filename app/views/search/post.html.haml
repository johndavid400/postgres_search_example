.container
  %h1 Search Posts
  .search
    = label_tag "To search, start typing below:"
    = text_field_tag :search, nil, :id => "searcher"
  #results

:javascript
  $("#searcher").keyup(function(event) {
    $.get('#{post_search_url}?search='+ $(this).val());
  });
